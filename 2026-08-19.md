# 2026-08-19 工作日志

## FindAixuexi 校区定位网页
- 新建 `FindAixuexi.html`（单文件应用，工作区根目录），数据来自 `C:\Users\13430\Desktop\工作日志.xlsx`「校区」sheet（22 个校区：海淀5/东城1/朝阳5/西城1/昌平10，负责人蒋洋洋、刘露露、杨晶雯、卜薪富、罗惠芳、王文文、刘宇、李虎飞、李阳、赵国庆、马梓钰）。
- 功能：进入自动定位 + 腾讯地图显示全部校区标记（按区域配色）；输入地址显示各校区距离并推荐最近 3 个（含负责人）；添加/删除校区（localStorage 持久化，键前缀 `fx_`：custom/deleted/geoCache/qr）；负责人微信二维码上传入口（canvas 压缩至 400px，base64 存 localStorage）；校区详情可「在地图上重新定位」（选点模式）。
- 技术要点：腾讯地图 GL JS 代理模式（`_TMapSecurityConfig` + `__WB_HTTP_PORT__`/`__WB_TMAP_SECRET__` 占位符原样保留，SDK 不带 key）；地址解析用 `TMap.service.Geocoder`，坐标缓存到 localStorage；距离为 Haversine 直线距离；数据合并策略：BUILTIN(内置) 排除 deletedIds + custom（用户添加）。
- 坑：① editor_sdk 无法打开该 xlsx（No workbook open），sheet-agent MCP 不可用 → 用 openpyxl（venv `C:\Users\13430\.workbuddy\binaries\python\envs\default`）兜底读取；② `allCampuses()` 每次生成新对象，运行时坐标/距离需存 `runtimeCache`，否则丢失；③ 地图容器在 flex 下必须显式高度（`calc(100vh - 56px)`），否则不渲染；④ 未定位校区不能塞 (0,0) 坐标，需过滤。

## FindAixuexi 修复迭代（同日）
- 修复地址解析：原 `TMap.service.Geocoder` 对简写/不精确地址（如「中关新园」）易失败，新增多重回退：原地址 → 补全「北京市」前缀 → 地点搜索 `TMap.service.Search`（region=北京市） → 带前缀的地点搜索；结果统一缓存到 `fx_geoCache`。
- 修复校区标记不显示：将并发解析改为串行（70ms 间隔）避免限流，全部解析完后批量 `setGeometries` 刷新一次；新增 `fitCampusBounds()` 在无定位/搜索原点时自动缩放地图以展示全部校区。
- 新增删除二次确认：用自定义模态框替代浏览器 `confirm`，含标题、正文、取消/删除按钮，支持点击遮罩和 ESC 关闭。
- 新增编辑校区：校区详情卡片增加「编辑」按钮；自定义校区直接修改 `fx_custom`，内置校区通过 `fx_overrides` 覆盖字段（name/address/district/manager/geo），实现持久化编辑。
- 修复添加校区弹窗关闭：关闭按钮改为绝对定位在弹窗右上角；取消按钮、`×`、点击遮罩、ESC 均可关闭；弹窗打开时自动清空上一详情选中状态，避免地图选点误更新旧校区。

## FindAixuexi 修复迭代（第 2 轮，同日）
- 修复编辑/添加弹窗无法关闭及地图穿透：给 `#map` 增加 `map-disabled` 类，弹窗打开时设置 `pointer-events:none`，关闭时恢复，避免地图继续接收鼠标事件导致无法关闭弹窗。
- 调整详情入口：删除详情卡片中的「在地图上重新定位」和「上传二维码」按钮；改地址、上传二维码统一通过「编辑」表单完成，提示文案同步改为「后续在编辑中补充」。
- 校正知春里校区坐标：将 geoCache 存储 key 从 `fx_geoCache` 升级为 `fx_geoCache_v2`，强制丢弃旧版错误缓存；编辑校区打开时自动重新解析地址，保存时更新坐标。
- 地址搜索联想：查询输入框增加 `input` 联想下拉，调用 `TMap.service.Search` 返回最多 5 条候选，支持下拉点击、键盘上下键/Enter/Esc；建议框使用 fixed 定位避免被 panel-body 裁剪。
- 搜索地点图标由红色改为绿色（`#16a34a`）。

## FindACZ 品牌升级与界面调整（第 3 轮，同日）
- 文件重命名：`FindAixuexi.html` → `FindACZ.html`，删除旧文件；文件内所有 `FindAixuexi` 字样替换为 `FindACZ`。
- Logo 替换：顶部蓝色方块+「爱」改为引用本地 `爱成长logo.png`（`logo-img` 高度 34px，宽度自适应），副标题同步改为「爱成长 · 校区定位」。
- 图标颜色统一为坐标水滴形状：当前位置黑色（`#1f2733`）、搜索位置红色（`#dc2626`）、校区统一蓝色（`#2f6fed`）；图例改为「校区 / 当前位置 / 查询位置」。
- 搜索联想修复：改用 `TMap.service.Search.suggestion` 输入提示接口，增强返回数据 normalize 与空结果提示；建议框 `z-index` 提升至 50。
- 查询输入框拉长：input 设置 `width:100%`，让输入框占满搜索行剩余宽度。
- 知春里坐标校正：geoCache 升级到 `fx_geoCache_v3`，强制丢弃旧错误缓存；编辑校区时自动重新解析地址。

## FindACZ 关键修复：Key 配置与跨浏览器同步（第 4 轮，同日）
- 问题根因：页面一直使用 WorkBuddy 预览代理占位符（`__WB_HTTP_PORT__`/`__WB_TMAP_SECRET__`），独立打开或部署到 GitHub 后占位符不会被替换，导致腾讯地图 Geocoder/Search 全部失败，表现为：地址解析失败、校区标记不显示、距离和推荐不渲染。
- 解决方案：移除代理占位符，改为用户自行配置腾讯地图 Key。新增「设置」面板（右上角 ⚙️），Key 保存在 `fx_tmapKey`；页面启动时动态加载带 Key 的腾讯地图 JS API（`https://map.qq.com/api/gljs?v=1.exp&libraries=service&key=...`）；无 Key 时地图区域显示引导，提示用户去设置。
- 跨浏览器数据同步：新增 GitHub Gist 云端同步。设置面板可填写 GitHub Token（需 `gist` 权限）和 Gist ID（留空自动创建）；启动时自动拉取云端数据覆盖本地；校区数据变化后 2 秒 debounce 自动推送到 Gist；同时提供「导出/导入 JSON」手动同步兜底。
- 兼容性优化：`extractLoc` 和 `fetchSuggestions` 的 normalize 兼容腾讯地图返回的 `TMap.LatLng` 函数对象与纯对象；`sSet` 增加 `skipSync` 参数避免拉取云端时触发反向推送。

## FindACZ 修复：删除确认层级 + 内置默认地图 Key（第 5 轮，同日）
- 修复删除校区确认弹窗无法点击取消/关闭：将 `#confirmMask` 的 `z-index` 从 110 提升到 1000，确保在所有弹窗/地图控件之上；给 `.confirm-modal` 和 `.confirm-actions .btn` 加 `position:relative` 与 `z-index`，避免被地图 canvas 等不可见层拦截点击。
- 防地图穿透：`showConfirm` 打开时给 `#map` 添加 `map-disabled`（`pointer-events:none`）；新增 `dismissConfirm()` 在确认关闭时判断，只有设置/编辑弹窗均未打开才恢复地图交互。
- 事件冒泡：取消、确定、遮罩关闭均调用 `stopPropagation()`，避免误触发其他层点击。
- 内置腾讯地图默认 Key：新增 `DEFAULT_TMAP_KEY`（腾讯地图官方示例 Key `OB4BZ-D4W3U-B7VVO-4PJWW-6TKDJ-WPB77`），页面启动直接调用 `loadTMapAndBoot(effectiveTmapKey())` 加载地图，无需用户先填写 Key；设置面板仍保留 Key 输入框，用户可填入自己的 Key 替换默认 Key，清空时自动恢复默认 Key。
- 保留 GitHub Gist 云端同步：设置面板中 GitHub Token/Gist ID 配置、自动拉取/推送、导出/导入 JSON 全部保留。

## FindACZ 修复：图标颜色改用 SVG + 地址解析增强（第 6 轮，同日）
- 图钉颜色未生效问题：`TMap.MarkerStyle` 的 `color` 参数在某些情况下不生效，导致所有图钉显示为默认红色。新增 `pinSvg(color)` 生成自定义水滴形 SVG marker，并分别应用：校区蓝色 `#2f6fed`、选中橙色 `#ff6b2b`、当前位置黑色 `#1f2733`、查询位置红色 `#dc2626`，颜色严格可控。
- 选中高亮：校区列表选中或地图点击校区时，对应 marker 切换为 `sel` 样式（橙色、尺寸更大）。
- 地址解析增强：用户搜索「中关新园」失败。重写 `geocode()`，新增 `makeCandidates()` 生成多种关键词候选（原词、加「北京市」前缀、加「 北京」后缀、去掉「园/小区/号楼/号院/社区/公寓/大厦/中心/广场」等后缀），依次尝试 `Geocoder.getLocation` 和 `Search` 解析，命中即缓存结果。
- `searchPlace()` 改为取前 5 条候选中第一条含坐标的记录，提高命中率。
- 其他功能保持不动，仅修改 marker 样式和 geocode 逻辑。

## FindACZ 地图引擎替换：腾讯地图 → 高德地图（第 7 轮，同日）
- 用户反馈腾讯示例 Key 持续被限流、地址解析仍失败、地图上 22 个校区图标不显示，要求参考「词汇家底炫富大赛」项目（实际无地图，仅距离排序）。决定放弃腾讯地图，整体替换为高德地图 JS API 2.0。
- 关键改动（通过 `_amap_upgrade.py` 脚本执行，已完成并删除脚本）：
  - SDK：`<script src="https://webapi.amap.com/loader.js"></script>` + `AMapLoader.load({key, version:"2.0", plugins:["AMap.Geocoder","AMap.AutoComplete","AMap.PlaceSearch"]})`；`_AMapSecurityConfig.securityJsCode` 支持安全密钥（2021-12-02 后申请的 Key 必填）。
  - 文件内已无任何 TMap / map.qq.com 残留（仅 `settings.amapKey` 初始化时兼容读取旧 `fx_tmapKey` 做迁移）。
  - 解析：`AMap.Geocoder({city:"北京市", batch:false}).getLocation()`；联想：`AMap.AutoComplete({city:"北京", citylimit:true})`；兜底搜索：`AMap.PlaceSearch({city:"北京", pageSize:5, citylimit:true})`；`extractLoc` 兼容 `location` 为 LngLat 对象或纯对象。
  - Marker：`new AMap.Marker({position:[lng,lat], icon: pinSvg(color,w,h), offset: new AMap.Pixel(-w/2,-h), title, extData})`，点击 `selectCampus(id)`；颜色规则不变（校区蓝 #2f6fed / 选中橙 #ff6b2b / 当前位置黑 #1f2733 / 查询红 #dc2626）。
  - 地图：`AMap.Map("map", {zoom:11, center:[116.34,40.02], viewMode:"2D"})`；`map.on("click")` 处理选点；`fitCampusBounds` 用 `AMap.Bounds`；用户 marker 用 `AMap.Pixel` 偏移。
  - 设置面板：`sAmapKey` + `sAmapSecret`（替换原腾讯 Key 输入），存 `fx_amapKey`/`fx_amapSecret`；无 Key 时地图区显示申请引导（console.amap.com）。
  - 高德没有公开可用的示例 Key，必须用户自行注册（免费），一次配置后存 localStorage 永久生效。
- 其他功能未动：GitHub Gist 同步、导出/导入 JSON、删除确认、编辑覆盖、搜索联想下拉、二维码上传、距离推荐。JS 语法已用 node --check 验证通过。

## FindACZ 移除地图显示：改为单列居中布局（第 8 轮，同日）
- 用户要求删除右侧地图，把「查询距离」放到中间、下面是所有校区，保留定位、增减校区等功能。
- 布局改动：`<main>` 从 flex 改为普通块级滚动，`#panel` 改为 `max-width:720px;margin:0 auto` 居中卡片；搜索行、定位状态、推荐、详情卡均限制最大宽度 600px 并居中；校区列表占满下方。
- 删除内容：右侧 `#map` 容器、图例、地图选点提示、所有 `AMap.Marker` / `pinSvg` / `refreshAllMarkers` / `fitCampusBounds` / `initMap` / `renderUserMarkers` 等地图渲染代码；`map-disabled` 与 `mapHint` 相关逻辑；弹窗不再操作地图穿透。
- 保留并继续使用的：高德 JS SDK 仅用于地址解析（`AMap.Geocoder`）和搜索联想（`AMap.AutoComplete`/`AMap.PlaceSearch`），因此仍需要用户配置一次高德 Key；浏览器定位、Haversine 直线距离、校区列表排序、推荐、增删改查、编辑覆盖、GitHub Gist 同步、导入导出、二维码上传全部保留。
- 文案调整：设置面板 hint、编辑表单 geo 状态、添加校区后提示等去掉「地图/重新定位」字样，改为提示编辑补充地址。
- 清理临时脚本 `_remove_map.py`、`_cleanup_map.py`、`_temp_check.js`。

## FindACZ 地图引擎换回腾讯 + 定位距离修复（第 9 轮，同日）
- 用户要求：① 定位后显示与各校区距离；② 地图换回腾讯地图（此前第 7 轮换成了高德）。
- 高德 → 腾讯 WebService API（HTTP 接口，非 GL JS SDK，因此无需地图容器）：
  - 删除 `webapi.amap.com/loader.js` 及全部 `AMap` 引用（grep 验证 0 残留）。
  - 地址解析 `geocoder/v1`；地点搜索 `place/v1/search`；联想 `place/v1/suggestion`；坐标转换 `coord/v1/translate`。
  - 设置面板：`sAmapKey/sAmapSecret` → `sTmapKey/sTmapSecret`（存 `fx_tmapKey`/`fx_tmapSecret`），保留腾讯 Key 申请链接 lbs.qq.com/dev/console/key/manage。
  - 新增 `tmapBuildUrl`（支持 SK 签名 `sig=md5("/ws/"+api+"?"+排序参数+"&sk="+SK)`）、`tmapJsonp`（fetch 在 file:// 下 CORS 失败时回退 JSONP）、`tmapReq`。
- **纯 JS MD5 实现坑**：4 轮 64 步每轮结束必须做「反馈相加」`(a+oa)>>>0` 等，漏掉则全部结果错误。已用 Python 参考实现逐字节比对修复，5 个标准测试向量全部 PASS。
- **腾讯官方示例 Key `OB4BZ-D4W3U-B7VVO-4PJWW-6TKDJ-WPB77` 已失效**：curl 实测 geocoder/suggestion/translate 均返回 `{"status":199,"message":"此key未开启WebserviceAPI功能"}`。用户必须自行申请 Key（免费），设置面板已有引导。
- **定位距离不依赖 Key 的兜底**：新增本地 WGS84→GCJ02 转换算法 `__wgs2gcjLocal`（公开算法，与 Python 标准实现交叉验证 0 误差，北京/上海/深圳/哈尔滨通过）。`wgs2gcj()` 首选腾讯 translate 接口，失败回退本地算法——保证示例 Key 失效时定位后距离显示依然准确。
- 修复 `doSearch` 中残留的 `showPickOnMap(ll)` 调用（函数已随地图删除，会导致搜索后刷新中断）。
- 定位按钮：`navigator.geolocation` 拿到 WGS84 → `wgs2gcj` 转 GCJ02 → `setMyLocation` → `refreshDistances` 按 Haversine 距离排序显示。
- 全部临时文件（_temp_check/_md5_*/_gcj_test/_check）已清理，JS 语法 node --check 通过。

## FindACZ 校区列表改为按区域分组（第 10 轮，同日）
- 用户要求「下面的校区按照区域排列」：`renderCampusList` 改为**始终按区域分组**（不再是无定位才分组）。
- 区域固定顺序 `DISTRICT_ORDER = [海淀区,东城区,朝阳区,西城区,昌平区]`，未列出的区域（自定义校区）依次排后。
- 组内排序：已定位/查询时按距离近→远（`_d`），否则保持内置顺序；每个校区仍显示距离徽标。
- 新增 `.dist-group` 样式（蓝色圆点 + 区域名 + 「N 所」计数徽章）。
- 顶部「最近校区」推荐区（recBox）逻辑不变，仍按距离取最近 3 个。
- 语法验证通过，无临时文件残留。

## FindACZ 搜索联想下拉补全（第 11 轮，同日）
- 用户反馈输入地址时没有像地图应用那样的地点推荐选项（截图样式：地点名 + 区县标签）。
- 补充完整联想逻辑：之前只有 DOM 容器和 CSS 框架，没有 `input` 触发和渲染函数。新增 `renderSuggestions`/`setActiveSuggestion`/`selectSuggestion`。
- 行为：输入≥1 个字时 250ms 防抖调用 `place/v1/suggestion`；下拉显示地点名 + 区县标签 + 详细地址；支持鼠标点击、键盘 ↑/↓ 选择、Enter 确认、Esc 关闭。
- 选中后直接拿 suggestion 的 GCJ02 坐标设置 `state.origin` 并 `refreshDistances`，避免二次 geocode，体验更快。
- 删除 `addrInput` 上旧的 Enter-only keydown，避免 suggestion 未显示时重复触发 `doSearch`。
- 修复建议项右侧空白点击无效：将 click 事件改为在 `.suggest-box` 上事件委托；item 上 mousedown 时 `preventDefault()` 阻止 input blur 导致下拉消失，同时保留滚动条可拖动（仅对 item 内部阻止默认行为）。
- 语法验证通过。
